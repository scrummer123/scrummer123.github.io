import React from "react";

const App: React.FC = () => (
  <div>
    <h1>test</h1>
    <button className="customButton">
      <span className="sr-only">View notifications</span>
    </button>
  </div>
);

export default App;
